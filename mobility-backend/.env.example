# Copy to .env and edit. Then run: go run ./cmd/server  (or make run)
# DB_* are read by the app; Makefile also loads .env for "make run"
DB_NAME=mobility
DB_USER=
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=5432
